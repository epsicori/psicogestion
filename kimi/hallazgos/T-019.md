# Hallazgos de T-019

## 1 · `coverage/` no está ignorada por ESLint en `main` — RESUELTO

- **Fichero**: `eslint.config.mjs`, `coverage/block-navigation.js:1`
- **Qué pasaba**: `npm run lint` terminaba con un aviso —`Unused eslint-disable
  directive`— sobre un fichero generado por la cobertura de Vitest.
- **Estado**: la corrección de la rama de T-016 (commit `3797cb0`, «Ignorar
  `coverage/` en ESLint») ya está integrada en `main`. Tras rebasar T-019 sobre
  `main` (f385a2c), `npm run lint` sale limpio, sin errores ni avisos. Nada que
  hacer.

## 2 · El linter sobre las migraciones existentes: cero hallazgos reales

- **Qué pasa**: `npm run lint:migraciones` sobre las **seis** migraciones de
  `main` (incluidas las tres de la enmienda multicentro y la auditoría de
  T-002/T-004, `20260829120000` a `20260829180000`) devuelve limpio:

  ```
  lint-migraciones: 6 migración(es) limpias.
  exit: 0
  ```

  Así que **ninguna regla se ha degradado a aviso** (`REGLAS_EN_AVISO` queda
  vacío). No hay nada que limpiar en otro ticket.
