# Hallazgos de T-019

## 1 · `coverage/` no está ignorada por ESLint en `main`

- **Fichero**: `eslint.config.mjs` (en `main`), `coverage/block-navigation.js:1`
- **Qué pasa**: `npm run lint` termina con un aviso —`Unused eslint-disable
  directive`— sobre un fichero generado por la cobertura de Vitest. No es
  error y no bloquea.
- **Por qué no lo arreglé**: `eslint.config.mjs` no está en el alcance de
  T-019. La rama de T-016 ya contiene la corrección (commit `3797cb0`),
  pendiente de integrar. Es el mismo hallazgo anotado en T-018.

## 2 · El linter sobre las migraciones existentes: cero hallazgos reales

- **Qué pasa**: `npm run lint:migraciones` sobre las tres migraciones de
  `main` devuelve limpio, así que **ninguna regla se ha degradado a aviso**
  (`REGLAS_EN_AVISO` queda vacío). No hay nada que limpiar en otro ticket.
