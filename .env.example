NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Clave de servicio de Supabase. NUNCA con prefijo NEXT_PUBLIC_ y NUNCA un valor real
# aquí: solo la usa lib/supabase/administracion.ts, en servidor, para la Admin API de
# Auth (invitar, MFA, baneo). Rellénala con `npx supabase status` en local.
SUPABASE_SERVICE_ROLE_KEY=
